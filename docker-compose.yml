services:
  backend:
    build: ./backend
    ports:
      - "8001:8001"
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./backend/app:/app/app
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
