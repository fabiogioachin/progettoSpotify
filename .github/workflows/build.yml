name: Build Docker Images

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build backend image
        run: docker build -t spotify-intelligence-backend ./backend

      - name: Build frontend image
        run: docker build -t spotify-intelligence-frontend ./frontend
